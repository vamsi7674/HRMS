# ═══════════════════════════════════════════
# WorkForce HRMS — Ansible Inventory
# ═══════════════════════════════════════════
# This file is AUTO-GENERATED by the Jenkins pipeline.
# The "Generate Ansible Inventory" stage discovers EC2 instances
# from the ALB target group and writes their private IPs here
# before the Ansible deployment runs.
#
# For MANUAL deployment, replace the placeholder below
# with actual EC2 private IPs:
#   ec2-instance-1 ansible_host=10.0.1.100
#   ec2-instance-2 ansible_host=10.0.2.100

[webservers]
# Populated dynamically by Jenkins pipeline

[webservers:vars]
ansible_user=ec2-user
ansible_ssh_common_args='-o StrictHostKeyChecking=no'
